# aqc_c

With Cython installed, running "python setup.py build_ext --inplace" should compile all the modules. Then "aqc_c" can be
imported and the function aqc_c.aqc can be used ("help(aqc_c.aqc)" will describe how).
